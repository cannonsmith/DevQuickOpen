/* When they hit the return/enter key with the option/alt key
down, we want the selected item's documentation file to be
opened. Or created and opened if it doesn't exist.
*/

DevQuickOpen_HandleSelected(True)
